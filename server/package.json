{
  "name": "benseno-server",
  "version": "0.2.0",
  "private": true,
  "description": "Benseno DB-backed backend — Postgres truth + read/write API (Canvas migrasyonu)",
  "type": "commonjs",
  "scripts": {
    "migrate": "node scripts/migrate.js",
    "db:ping": "node server/scripts/db-ping.js",
    "seed": "node server/scripts/seed-from-livedata.js",
    "api": "node server/api.js",
    "migrate:status": "node scripts/migrate.js status"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.13.1",
    "zod": "^3.24.1",
    "dotenv": "^16.4.7",
    "jsonwebtoken": "^9.0.3",
    "bcryptjs": "^3.0.3"
  }
}
