# P3.4a — Kendi Performansını Görme Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Çalışan kendi Profil sayfasında kendi tamamlanan işlerinin ⭐ puanını, AI sebep metnini ve bnsKisiPerformans özetini görür (yalnız kendi verisi, salt-okunur).

**Architecture:** Yalnız frontend (Profile.jsx): `_isSelf` gate'li yeni "Performansım" kartı. Veri `allCompletedRaw`'dan kişi filtresiyle; hesap P3.3b `bnsKisiPerformans` (calc, testli); yıldızlar Completed.jsx'teki `Stars` bileşeni `onRate`'siz (salt-okunur). Backend/migration yok.

**Tech Stack:** React UMD/JSX, calc.js (mevcut), esbuild bundle.

---

## Referans

- `Stars({ n, onRate, ai, sebep })` — Completed.jsx:189, top-level → bundle'da global. `onRate` verilmezse tıklanamaz (cursor default).
- Profile.jsx: `u` = görüntülenen kişi, `_isSelf` (satır ~243), `allCompletedRaw` (satır ~86, tarih-filtresiz tüm tamamlananlar).
- `bnsKisiPerformans(completed, now)` → `{tamamlanan, ortDonguH, zamanindaPct, ortRevize, ortPuan, tipDagilim, throughputHaftalik}` (calc.js, 7 birim test).
- Tamamlanan brief alanları: `no, marka, baslik, rating, rating_sebep, sureH, deadline, bitis, rev_ic, rev_musteri, workers[], leads[]` (id'li nesneler).

## Task 1: Performansım kartı + İşlerim listesi (Profile.jsx)

**Files:**
- Modify: `dashboard/app/screens/Profile.jsx`

- [ ] **Step 1: Bileşeni ekle (dosyanın üstüne, AyarlarBolumu'ndan sonra)**

```jsx
// ─── P3.4a: Kendi performansını görme (Ç1) — yalnız kendi profilinde (_isSelf) ───────
// Veri zaten akıyor (SEC-4 gevşek); bu kart görünürlüğü KENDİ verisi için açar.
function KendiPerformansKarti({ u, allCompleted }) {
  // Kişinin worker VEYA lead olduğu tamamlananlar (alan adı esnek: workers/contributors)
  const benim = (allCompleted || []).filter(b => {
    const ppl = [...(b.workers || b.contributors || []), ...(b.leads || [])];
    return ppl.some(p => p && p.id === u.id);
  });
  if (!benim.length) return null;
  const P = (typeof bnsKisiPerformans === "function") ? bnsKisiPerformans(benim, Date.now()) : null;
  const topMarka = P ? Object.entries(P.tipDagilim).sort((a,b) => b[1]-a[1]).slice(0,3) : [];
  const son20 = benim.slice().sort((a,b) => (b.bitis||0)-(a.bitis||0)).slice(0,20);
  const K = ({ v, l }) => (
    <div style={{textAlign:"center"}}>
      <div style={{font:"700 18px/1.2 var(--font-sans)"}}>{v ?? "—"}</div>
      <div style={{font:"400 11px/1.4 var(--font-sans)", color:"var(--ink-3)"}}>{l}</div>
    </div>
  );
  return (
    <Card style={{padding:16, marginTop:16}}>
      <div style={{font:"600 13px/1 var(--font-sans)", marginBottom:12}}>⭐ Performansım</div>
      {P && (
        <div style={{display:"grid", gridTemplateColumns:"repeat(auto-fit, minmax(90px, 1fr))", gap:10, marginBottom:14}}>
          <K v={P.tamamlanan} l="tamamlanan"/>
          <K v={P.ortPuan != null ? P.ortPuan : "—"} l="ort. puan"/>
          <K v={P.zamanindaPct != null ? "%" + P.zamanindaPct : "—"} l="zamanında"/>
          <K v={P.ortRevize} l="ort. revize"/>
          <K v={P.ortDonguH != null ? P.ortDonguH + "sa" : "—"} l="ort. döngü"/>
          <K v={P.throughputHaftalik} l="iş/hafta"/>
        </div>
      )}
      {topMarka.length > 0 && (
        <div style={{font:"400 12px/1.5 var(--font-sans)", color:"var(--ink-2)", marginBottom:12}}>
          En çok: {topMarka.map(([m,n]) => `${m} (${n})`).join(" · ")}
        </div>
      )}
      <div style={{font:"600 12px/1 var(--font-sans)", margin:"4px 0 8px"}}>İşlerim ve puanlarım</div>
      {son20.map(b => (
        <div key={b.id || b.no} style={{display:"flex", gap:8, alignItems:"baseline", padding:"6px 0", borderTop:"1px solid var(--line)"}}>
          <span style={{font:"500 12px/1.4 var(--font-sans)", whiteSpace:"nowrap"}}>#{b.no} {b.marka || ""}</span>
          <span style={{font:"400 12px/1.4 var(--font-sans)", color:"var(--ink-2)", flex:1, overflow:"hidden", textOverflow:"ellipsis", whiteSpace:"nowrap"}}>{b.baslik || ""}</span>
          {b.rating > 0
            ? <Stars n={b.rating} ai={b.rating_by === "ai"} sebep={b.rating_sebep || null}/>
            : <span style={{font:"400 11px/1.4 var(--font-sans)", color:"var(--ink-3)"}}>henüz puanlanmadı</span>}
        </div>
      ))}
      <div style={{font:"400 11px/1.6 var(--font-sans)", color:"var(--ink-3)", marginTop:8}}>
        Puanın sebebini görmek için yıldızların üzerine gel. Puanlar yönetici tarafından belirlenir.
      </div>
    </Card>
  );
}
```
Not: `rating_sebep` uzun olabilir — `Stars`'ın `title` tooltip'i zaten sebebi gösteriyor (Completed deseni), satırda ayrıca metin basmıyoruz. `Stars`'a `onRate` GEÇME (salt-okunur).

- [ ] **Step 2: Render et (self bölümüne)**

ProfileScreen gövdesinde, kişisel iş kuyruğu/özet bölümlerinin bittiği uygun yere (mevcut `_isSelf`/`u.id === currentUser.slack_id` gate'li bölümlerin yanına):
```jsx
{_isSelf && <KendiPerformansKarti u={u} allCompleted={allCompletedRaw}/>}
```

- [ ] **Step 3: CI (JSX parse)**

Run: `bash scripts/ci-check.sh 2>&1 | grep -E "jsx|derlen|❌|GEÇTİ"`
Expected: `✅ tüm .jsx derlendi` + `🟢 CI KAPISI GEÇTİ`.

- [ ] **Step 4: Commit**

```bash
git add dashboard/app/screens/Profile.jsx
git commit -m "feat(p3.4a): Performansım kartı — kendi puan+sebep+metrikler (Ç1, salt-okunur)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

## Task 2: Build + preview doğrulama + deploy

- [ ] **Step 1: Build**

Run: `bash scripts/build-dashboard.sh`
Expected: bundle + app/ + EMBEDDED senkron.

- [ ] **Step 2: Preview doğrulama (launch.json `dashboard`)**

preview_start → preview_eval ile:
- `typeof KendiPerformansKarti === "function"` (bundle'da var)
- Konsolda hata yok
- Sentetik doğrulama: tamamlanan işi olan bir kullanıcı için filtre+calc zinciri değer üretiyor
(Not: preview'da login yok → `_isSelf` canlıda kullanıcıyla test edilir; preview bundle bütünlüğünü doğrular.)

- [ ] **Step 3: Build commit + deploy**

```bash
git add -A
git commit -m "build(p3.4a): dashboard bundle senkron (Performansım kartı)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
bash scripts/deploy.sh dashboard
```
Expected: `🟢 deploy.sh tamam (dashboard)`.

## Self-Review

- Spec kapsama: Performansım kartı ✓ (Task 1 Step 1), İşlerim listesi + salt-okunur Stars ✓, `_isSelf` gate ✓ (Step 2), boş-veri gizleme ✓ (`if (!benim.length) return null`), sebep tooltip ✓, backend yok ✓.
- Placeholder yok; `Stars`/`bnsKisiPerformans`/`allCompletedRaw`/`Card` mevcut tanımlarla tutarlı.
- Bilinçli esneklik: workers/contributors alan-adı fallback'i (aktif/tamamlanan hydrate farkına karşı).
