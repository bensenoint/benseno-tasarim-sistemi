# P3.2 — Ody'yi Akıllandır — Uygulama Planı

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps `- [ ]`.

**Goal:** Ody'yi zenginleştir: V1 feedback öğrenme (ody_advice_feedback→prompt), V7 müşteri-risk trendi (brand_daily→marka_risk tool), V9 thread duygu tonu (thread-ozet→thread_ton). Yöneticiye push YOK (P3.3).

**Architecture:** Canlı DB okuma + mevcut LLM çağrılarına alan ekleme. Saf yardımcılar test edilir. Ton/risk yönetici-only.

**Spec:** `docs/superpowers/specs/2026-07-07-p3.2-ody-akillandir-design.md`

**Bağlam (doğrulandı):**
- `ody_advice_feedback` (vote up/down, reason, outcome, advice_text) — migration 0002.
- `brand_daily (brand_id, tarih, ozet, insight)` UNIQUE(brand_id,tarih) — `queries.js:288` insight okuyor; `api.js:908` yazıyor.
- `/api/briefs/:id/thread-ozet` (api.js:483, botGuard) `thread_ozet/at/ts` yazıyor; `scripts/thread-ozet.js:61` `{ozet,last_ts}` POST'luyor, Haiku ile özetliyor.
- ody-tools `ctx={user,isAdmin,ed}`; `is_detay` tool var (`:265`). Migration runner `server/scripts/migrate.js`, son 0013 → yeni 0014.

---

### Task 1: Migration 0014

**Files:** Create `server/migrations/0014_ody_akil.sql`
- [ ] **Step 1:**
```sql
-- P3.2: thread duygu tonu + marka risk seviyesi
ALTER TABLE briefs ADD COLUMN IF NOT EXISTS thread_ton TEXT;         -- notr|gergin|memnun|acil (yönetici-only)
ALTER TABLE brand_daily ADD COLUMN IF NOT EXISTS risk_seviye TEXT;   -- dusuk|orta|yuksek (marka_risk tool cache)
```
- [ ] **Step 2: Commit** `git add server/migrations/0014_ody_akil.sql && git commit -m "feat(P3.2): migration 0014 — thread_ton + brand_daily.risk_seviye"` (çalıştırma — deploy fazında).

---

### Task 2: V1 — Feedback öğrenme döngüsü (TDD saf yardımcı + enjeksiyon)

**Files:** Modify `server/ody-tools.js` (helper), `server/api.js` (chat prompt), `scripts/ody-icgoru.js`; Create `server/p3.2-feedback.test.js`

- [ ] **Step 1: Test** — `server/p3.2-feedback.test.js`:
```js
'use strict';
const test = require('node:test');
const assert = require('node:assert');
const { bnsFeedbackOzet } = require('./ody-tools');

test('boş → boş string', () => { assert.equal(bnsFeedbackOzet([]), ''); });
test('downvote sebeplerini derler, 400 kırpar', () => {
  const rows = [{ reason: 'çok genel', advice_text: 'x' }, { reason: 'yanlış kişi', advice_text: 'y' }];
  const s = bnsFeedbackOzet(rows);
  assert.ok(s.includes('çok genel') && s.includes('yanlış kişi'));
  assert.ok(s.length <= 400);
});
```
- [ ] **Step 2: RED** — `node --test server/p3.2-feedback.test.js` → FAIL.
- [ ] **Step 3: ody-tools.js'e helper + export**:
```js
function bnsFeedbackOzet(rows) {
  const parts = (rows || []).map(r => (r.reason || r.advice_text || '').trim()).filter(Boolean);
  if (!parts.length) return '';
  let s = parts.slice(0, 8).join(' · ');
  return s.length > 400 ? s.slice(0, 397) + '…' : s;
}
```
`module.exports`'a `bnsFeedbackOzet` ekle.
- [ ] **Step 4: GREEN** — test 2/2.
- [ ] **Step 5: api.js chat enjeksiyonu** — chat handler'da sistem promptu kurulmadan ÖNCE downvote'ları çek, prompt'a ekle:
```js
    // V1: geçmiş beğenilmeyen öneriler → tekrarı önle
    let fbBlok = '';
    try {
      const fb = await pool.query(`SELECT advice_text, reason FROM ody_advice_feedback WHERE vote='down' ORDER BY created_at DESC LIMIT 20`);
      const oz = odyTools.bnsFeedbackOzet(fb.rows);
      if (oz) fbBlok = `\n## GEÇMİŞ GERİ BİLDİRİM\nKullanıcılar şu tarz önerileri beğenmedi (tekrarlama, farklı/daha somut yaklaş): ${oz}\n`;
    } catch (e) {}
```
`fbBlok`'u sistem promptu string birleşimine ekle (nitel-yönerge yakınına).
- [ ] **Step 6: ody-icgoru.js enjeksiyonu** — içgörü üretiminde (SYS/prompt) aynı sorguyu çalıştırıp SYS'e "geçmişte şu öneriler beğenilmedi: …" ekle (pool zaten var). Kısa tut.
- [ ] **Step 7: `node --check` (api.js, ody-tools.js, ody-icgoru.js) + `node --test server/ody-tools.test.js server/p3.2-feedback.test.js` yeşil.**
- [ ] **Step 8: Commit** `git add server/ody-tools.js server/api.js scripts/ody-icgoru.js server/p3.2-feedback.test.js && git commit -m "feat(P3.2 V1): feedback öğrenme — downvote'lar prompt'a (chat + içgörü)"`

---

### Task 3: V9 — Thread duygu tonu

**Files:** Modify `scripts/thread-ozet.js`, `server/api.js` (thread-ozet endpoint), `server/ody-tools.js` (is_detay), `dashboard/app/BriefDrawer.jsx`

- [ ] **Step 1: thread-ozet.js — LLM'e ton ekle** — OKU. Özet üreten Haiku çağrısının prompt'una "ayrıca bu thread'in tonunu tek kelimeyle belirle: notr|gergin|memnun|acil (emin değilsen notr)" ekle; yanıttan `ton` ayrıştır (JSON döndürtmek en temiz: `{ozet, ton}`). `saveOzet`'e `ton` geçir; POST gövdesine `ton` ekle.
- [ ] **Step 2: api.js /thread-ozet — thread_ton yaz** — endpoint (api.js:483) body'den `ton` al, UPDATE'e ekle: `thread_ton = $N` (enum doğrula: notr|gergin|memnun|acil değilse null).
- [ ] **Step 3: is_detay tool — ton döndür** — ody-tools.js `is_detay` run'ında brief'in `thread_ton`'unu (ed'den veya sorgudan) yalnız yönetici iken çıktıya ekle.
- [ ] **Step 4: BriefDrawer rozeti** — yönetici (`_isMgr` deseni) + `b.thread_ton` varsa küçük rozet: notr 😐 / gergin 😟 / memnun 🙂 / acil 🔥 + tooltip.
- [ ] **Step 5: `node --check` + `bash scripts/ci-check.sh` 🟢.**
- [ ] **Step 6: Commit** `git add scripts/thread-ozet.js server/api.js server/ody-tools.js dashboard/app/BriefDrawer.jsx && git commit -m "feat(P3.2 V9): thread duygu tonu (thread-ozet→thread_ton, is_detay + drawer rozeti, yönetici-only)"`

---

### Task 4: V7 — marka_risk Ody tool + Marka detay rozeti

**Files:** Modify `server/ody-tools.js`, `dashboard/app/screens/Brand.jsx`

- [ ] **Step 1: marka_risk tool** (ody-tools.js, `const TOOLS`'tan önce):
```js
defs.marka_risk = {
  description: "Marka sağlık/müşteri-risk trendi (YALNIZ yönetici). Son ~5 günün gün-sonu insight'ından risk seviyesi (dusuk/orta/yuksek) + tek cümle gerekçe. marka gerekir.",
  input_schema: { type: 'object', required: ['marka'], properties: { marka: { type: 'string' } } },
  run: async (input, ctx) => {
    const me = (ctx.ed.bns_users || []).find(u => u && u.id === (ctx.user && ctx.user.slack_id));
    if (!(ctx.isAdmin || (me && me.rol === 'yonetici'))) return { hata: 'bu bilgi yöneticilere özel' };
    // Son 5 günün insight'ı — marka adından brand_id çöz + brand_daily
    const q = await pool.query(
      `SELECT d.tarih, d.insight FROM brand_daily d JOIN brands b ON b.id=d.brand_id
       WHERE b.name ILIKE $1 AND d.insight IS NOT NULL ORDER BY d.tarih DESC LIMIT 5`, [`%${input.marka}%`]);
    if (q.rows.length < 2) return { risk: 'yetersiz veri', gerekce: 'son günlerde yeterli insight yok' };
    // LLM ile sınıflandır (Sonnet, kısa)
    const metin = q.rows.map(r => `${r.tarih}: ${r.insight}`).join('\n');
    // ... Anthropic çağrısı (api.js finans/insight deseni): system "son 5 günün marka insight'ına bak; risk dusuk/orta/yuksek + 1 cümle gerekçe; JSON {risk, gerekce}"; başarısız → {risk:'bilinmiyor'}
    return { marka: input.marka, ...parsed };
  },
};
```
NOT: `pool` ody-tools.js'te yok — bu tool DB gerektiriyor. `const { pool } = require('./db')` ekle (yalnız bu tool için; diğer tool'lar ed kullanıyor). Anthropic çağrısı için api.js'teki fetch desenini küçük yerel fonksiyona al (veya ody-tools'a `require`). Cache: hesaplanan risk `brand_daily.risk_seviye`'ye (en güncel tarih satırına) yazılabilir — aynı gün tekrar LLM'e gitmemek için önce onu kontrol et.
- [ ] **Step 2: Brand.jsx rozeti** — yönetici iken marka detayında risk rozeti (🟢/🟡/🔴 + gerekçe). Veri: ya `/api/embedded`'e marka risk_seviye eklenir ya da dashboard `marka_risk`'i Ody üzerinden çağırmaz — basit yol: `brand_daily.risk_seviye`'yi embedded'e ekleyip Brand'de göster (LLM'i dashboard tetiklemesin; cron/Ody hesaplar). MVP: rozet yalnız `risk_seviye` doluysa göster; hesaplama Ody `marka_risk` çağrısında olur. (İlk sürümde dashboard rozeti opsiyonel — Ody tool yeterli; Brand rozetini `risk_seviye` embedded'de varsa göster.)
- [ ] **Step 3: `node --check` + `bash scripts/ci-check.sh` 🟢.**
- [ ] **Step 4: Commit** `git add server/ody-tools.js dashboard/app/screens/Brand.jsx server/queries.js && git commit -m "feat(P3.2 V7): marka_risk tool (müşteri-risk trendi, yönetici-only) + marka rozeti"`

---

### Task 5: Build + deploy + migration + doğrulama

- [ ] **Step 1:** `bash scripts/ci-check.sh && bash scripts/build-dashboard.sh` → 🟢.
- [ ] **Step 2:** `bash scripts/deploy.sh api && bash scripts/deploy.sh dashboard`.
- [ ] **Step 3: Migration 0014** — yeni image sonrası `railway ssh --service benseno-api "node scripts/migrate.js"` → 0014 ✓.
- [ ] **Step 4: Doğrulama** — Ody yöneticiye `marka_risk` döner (düz üye "yöneticilere özel"); yeni tamamlanan işte `thread_ton` doluyor (thread-ozet cron sonrası); downvote varsa feedback bloğu prompt'a giriyor; ton/kâr rozetleri yalnız yönetici.

---

## Self-Review Notları
- **Spec kapsamı:** V1 (Task 2), V9 (Task 3), V7 (Task 4), migration (Task 1), deploy (Task 5). Proaktif push YOK (P3.3) — uyumlu.
- **İsim tutarlılığı:** `bnsFeedbackOzet` (ody-tools export + test + chat/içgörü kullanımı); `thread_ton` (migration + thread-ozet + endpoint + is_detay + drawer); `risk_seviye`/`marka_risk` (migration + tool + Brand).
- **Şüpheli:** ody-tools.js'e `pool` require'ı (marka_risk için) — diğer tool'lar ed kullanıyor; yalnız bu tool DB'ye gidiyor, teyit. Anthropic çağrısı için ody-tools'ta fetch — api.js deseniyle aynı. V7 dashboard rozeti MVP'de opsiyonel (embedded'de risk_seviye varsa).
</content>
