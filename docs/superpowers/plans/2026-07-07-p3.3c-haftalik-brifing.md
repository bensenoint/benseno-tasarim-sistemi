# P3.3c — Haftalık GM Brifingi Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Pazartesi 08:00 tüm firma sinyallerini + finans özetini Opus ile sentezleyip yönetici+GM'e hafta-önü brifingi DM eden bir script kurmak; Opus yoksa deterministik fallback gönderilir.

**Architecture:** Yeni `scripts/firma-brifing.js` — `firma-sinyal.js`'ten `sinyalleriHesapla`+`yoneticiMi`'yi, calc'tan `bnsFinansOzet`'i yeniden kullanır (DRY). Olguları Opus'a verir; başarısızsa `fallbackMetin` deterministik brifing üretir. Yönetici+GM'e `tip_firma_sinyal` toggle'ıyla DM.

**Tech Stack:** Node.js, rapor-lib (Slack + embedded), Anthropic Opus, Postgres (notify_prefs), node-cron.

---

## Referans / kısıtlar

- **Opus model:** `process.env.ODY_OPUS_MODEL || 'claude-opus-4-7'` (codebase deseni, hesapta erişilebilir).
- **Anthropic çağrı şekli** (rapor-lib `commentary`'den): `POST https://api.anthropic.com/v1/messages`, headers `{content-type, x-api-key: ANTHROPIC_API_KEY, anthropic-version: '2023-06-01'}`, body `{model, max_tokens, system, messages}`.
- **commentary guard eki** (aynen kullan): `' ÖNEMLİ: Yalnızca aşağıdaki olgulara dayan. Tarih, ay, proje türü (bu bir tasarım/reklam ajansıdır — yazılım değil), müşteri veya OLMAYAN hiçbir bilgi UYDURMA. Emin olmadığını yazma.'`
- **DRY kaynakları:** `firma-sinyal.js` → `sinyalleriHesapla(d, now)` (async, --dry'da DB'siz), `yoneticiMi(u)`; `calc.js` → `bnsFinansOzet(briefs)`.
- **finans:** tamamlanan işlerden (`d.bns_completed`) — kâr/marj + faturalanmamış/tahsil edilmemiş (fetchEmbedded bot token → sensitive alanlar var).
- `require('../dashboard/app/calc')` yalnız scripts/'te (scheduler imajı `COPY . .`). server/'a taşıma.
- **Migration yok.** `tip_firma_sinyal` (P3.3a) toggle'ı yeniden kullanılır.

---

## Task 1: firma-brifing.js

**Files:**
- Create: `scripts/firma-brifing.js`

- [ ] **Step 1: Dosyayı yaz**

`scripts/firma-brifing.js`:
```js
'use strict';
/**
 * firma-brifing.js — Haftalık GM brifingi (P3.3c). Pazartesi 08:00. Tüm firma sinyalleri +
 * finans → Opus sentezi (yoksa deterministik fallback) → yönetici+GM DM.
 * DRY: firma-sinyal.js sinyalleriHesapla + calc bnsFinansOzet.
 *   node scripts/firma-brifing.js         → canlı (BNS_REPORT_LIVE=1 DM)
 *   node scripts/firma-brifing.js --dry    → DM/LLM yok; olgular + hedef yazdır
 */
const { token, post, fetchEmbedded, GORKEM, DASHBOARD_URL } = require('./rapor-lib');
const { pool } = require('../server/db');
const { sinyalleriHesapla, yoneticiMi } = require('./firma-sinyal');
const C = require('../dashboard/app/calc.js');

const DRY = process.argv.includes('--dry');
const OPUS = process.env.ODY_OPUS_MODEL || 'claude-opus-4-7';
const GUARD = ' ÖNEMLİ: Yalnızca aşağıdaki olgulara dayan. Tarih, ay, proje türü (bu bir tasarım/reklam ajansıdır — yazılım değil), müşteri veya OLMAYAN hiçbir bilgi UYDURMA. Emin olmadığını yazma.';
const SYS = 'Sen Ody, Benseno iş asistanısın. Yöneticilere HAFTA-ÖNÜ stratejik brifing yaz: bu hafta neye dikkat edilmeli. Verilen sinyalleri ve finansı yorumla; en kritik 3-5 maddeyi öne çıkar, kısa ve eyleme yönelik ol. Başlık + madde imleri kullan, en fazla ~250 kelime. Türkçe.' + GUARD;

// Saf olgu toplama (Opus'a ve fallback'e girdi)
async function brifingOlgulari(d, now) {
  const sinyaller = await sinyalleriHesapla(d, now);
  const finans = C.bnsFinansOzet(d.bns_completed || []);
  return { sinyaller, finans, aktifSayi: (d.bns_briefs || []).length, tamamlananSayi: (d.bns_completed || []).length };
}

// Deterministik fallback (Opus yok/hatalı)
function fallbackMetin(o) {
  const f = o.finans || {};
  const grup = {};
  (o.sinyaller || []).forEach((s) => { grup[s.tip] = (grup[s.tip] || 0) + 1; });
  const satirlar = (o.sinyaller || []).map((s) => '• ' + s.text);
  return [
    '📋 *Haftalık firma brifingi* (hafta-önü)',
    '',
    `🚨 Sinyaller (${(o.sinyaller || []).length}):`,
    ...(satirlar.length ? satirlar : ['• Bu hafta kritik sinyal yok.']),
    '',
    '💰 Finans (tamamlanan):',
    `• Kâr: ${f.kar != null ? f.kar : '—'} · Marj: ${f.marj != null ? '%' + f.marj : '—'}`,
    `• Faturalanmamış: ${f.faturalanmamis || 0} · Tahsil edilmemiş: ${f.tahsilEdilmemis || 0}`,
    '',
    `🔗 ${DASHBOARD_URL}`,
  ].join('\n');
}

async function opusBrifing(o) {
  const key = process.env.ANTHROPIC_API_KEY;
  if (!key) { console.log('  (ANTHROPIC_API_KEY yok — fallback)'); return null; }
  try {
    const r = await fetch('https://api.anthropic.com/v1/messages', {
      method: 'POST',
      headers: { 'content-type': 'application/json', 'x-api-key': key, 'anthropic-version': '2023-06-01' },
      body: JSON.stringify({ model: OPUS, max_tokens: 800, system: SYS,
        messages: [{ role: 'user', content: 'Olgular (JSON):\n' + JSON.stringify(o, null, 2) }] }),
    });
    const j = await r.json().catch(() => ({}));
    if (!r.ok) { console.log(`  (Opus hata: ${j.error?.message || r.status} — fallback)`); return null; }
    const txt = (j.content || []).filter((c) => c.type === 'text').map((c) => c.text).join('').trim();
    return txt || null;
  } catch (e) { console.log(`  (Opus exception: ${e.message} — fallback)`); return null; }
}

async function main() {
  const tok = token();
  if (!tok && !DRY) { console.error('SLACK token yok'); process.exit(1); }
  const live = process.env.BNS_REPORT_LIVE === '1';
  const now = Date.now();
  const d = await fetchEmbedded();
  const olgular = await brifingOlgulari(d, now);

  const ids = Array.from(new Set(
    (d.bns_users || []).filter((u) => /^U/.test(u.id) && yoneticiMi(u)).map((u) => u.id).concat(GORKEM)));

  if (DRY) {
    console.log(`firma-brifing DRY — ${olgular.sinyaller.length} sinyal, ${ids.length} hedef`);
    console.log('  hedefler:', ids.join(', '));
    console.log(fallbackMetin(olgular));
    await pool.end();
    return;
  }

  const metin = (await opusBrifing(olgular)) || fallbackMetin(olgular);
  const full = metin + `\n\n🔗 ${DASHBOARD_URL}`;

  const prefs = new Map((await pool.query(
    `SELECT user_id, tip_firma_sinyal FROM notify_prefs`)).rows.map((r) => [r.user_id, r]));
  let sent = 0;
  for (const uid of ids) {
    const p = prefs.get(uid);
    if (p && p.tip_firma_sinyal === false) continue;
    if (live) { if (await post(tok, uid, full)) sent++; }
  }
  console.log(`firma-brifing ${live ? 'CANLI' : 'TEST'} — ${sent} DM`);
  await pool.end();
}

if (require.main === module) main().catch((e) => { console.error('firma-brifing hata:', e.message); process.exit(1); });
module.exports = { brifingOlgulari, fallbackMetin };
```

Not: `opusBrifing` sonunda DASHBOARD_URL zaten `full`'e eklenir; `fallbackMetin` de kendi içinde ekliyor → fallback yolunda çift link olmasın diye `full`'ü yalnız Opus metnine ekle. **Düzeltme:** `fallbackMetin`'den son `🔗` satırını çıkar (link'i `main` ekliyor). Aşağıdaki Step 2 testinde bu doğrulanır.

- [ ] **Step 2: fallbackMetin'den çift link'i temizle**

`fallbackMetin` dönüşünden son iki eleman (`''` ve `` `🔗 ${DASHBOARD_URL}` ``) çıkarılır — link `main`'de `full`'e eklenir. Son hâli:
```js
  return [
    '📋 *Haftalık firma brifingi* (hafta-önü)',
    '',
    `🚨 Sinyaller (${(o.sinyaller || []).length}):`,
    ...(satirlar.length ? satirlar : ['• Bu hafta kritik sinyal yok.']),
    '',
    '💰 Finans (tamamlanan):',
    `• Kâr: ${f.kar != null ? f.kar : '—'} · Marj: ${f.marj != null ? '%' + f.marj : '—'}`,
    `• Faturalanmamış: ${f.faturalanmamis || 0} · Tahsil edilmemiş: ${f.tahsilEdilmemis || 0}`,
  ].join('\n');
```

- [ ] **Step 3: Sözdizimi + fallback birim testi**

Run:
```bash
node --check scripts/firma-brifing.js
node -e "
const { fallbackMetin } = require('./scripts/firma-brifing');
const m = fallbackMetin({ sinyaller:[{tip:'burnout',text:'🔥 Eda %140'},{tip:'marka_risk',text:'📉 Acme'}], finans:{kar:1200,marj:35,faturalanmamis:500,tahsilEdilmemis:0} });
const ok = m.includes('Sinyaller (2)') && m.includes('Eda') && m.includes('Kâr: 1200') && m.includes('%35') && !m.includes('undefined');
console.log('fallback testi:', ok ? 'GEÇTİ' : 'KALDI');
console.log(m);
if(!ok) process.exit(1);
"
```
Expected: `fallback testi: GEÇTİ`; metinde sinyal satırları + finans; `undefined` yok.

- [ ] **Step 4: Wiring testi (brifingOlgulari, sentetik, --dry)**

Run:
```bash
node -e "
process.argv.push('--dry');
const { brifingOlgulari } = require('./scripts/firma-brifing');
const now = Date.now();
const d = {
  bns_users:[{id:'U1',rol:'yonetici'}],
  bns_briefs:[{no:1,marka:'A',deadline:now-3600000,durum:'devam',workers:[],leads:[]}],
  bns_completed:[{marka:'A',sureH:5,satis:1000,maliyet:600,fatura:true,odeme:false,rating:5,bitis:now}],
};
brifingOlgulari(d, now).then(o=>{
  console.log('sinyal:', o.sinyaller.length, '| finans kar:', o.finans.kar, '| marj:', o.finans.marj, '| tahsilEdilmemis:', o.finans.tahsilEdilmemis);
});
"
```
Expected: finans kar=400, marj=40, tahsilEdilmemis=1000 (fatura kesilmiş, ödenmemiş); hata yok.

- [ ] **Step 5: Commit**

```bash
git add scripts/firma-brifing.js
git commit -m "feat(p3.3c): haftalık GM brifingi script (Opus sentezi + deterministik fallback)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: Wrapper + scheduler cron

**Files:**
- Create: `scripts/run-firma-brifing.sh`
- Modify: `scripts/scheduler.js`

- [ ] **Step 1: Wrapper (run-firma-sinyal.sh desenini kopyala)**

`scripts/run-firma-brifing.sh`:
```sh
#!/bin/zsh
# Haftalık GM brifingi (P3.3c) — Pazartesi 08:00. Opus sentezi, yönetici+GM DM.
cd ~/benseno-tasarim-sistemi
export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"; source ~/.zshrc 2>/dev/null
node scripts/firma-brifing.js >> logs/firma-brifing.log 2>&1
```
Sonra: `chmod +x scripts/run-firma-brifing.sh`.

- [ ] **Step 2: scheduler.js'e Pazartesi cron ekle**

`scripts/scheduler.js` NOTIFY_V2 bloğunda, firma-sinyal cron'larının yanına:
```js
  // P3.3c: haftalık GM brifingi — Pazartesi 08:00
  cron.schedule('0 8 * * 1', () => run('run-firma-brifing.sh'), opts);
```

- [ ] **Step 3: Sözdizimi**

Run: `node --check scripts/scheduler.js`
Expected: çıktı yok.

- [ ] **Step 4: Commit**

```bash
git add scripts/run-firma-brifing.sh scripts/scheduler.js
git commit -m "feat(p3.3c): haftalık brifing cron (Pzt 08:00) + wrapper

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: Entegrasyon — CI + deploy + canlı doğrulama

- [ ] **Step 1: CI (calc/JSX değişmedi ama kapıyı çalıştır)**

Run: `bash scripts/ci-check.sh`
Expected: `🟢 CI KAPISI GEÇTİ`.

- [ ] **Step 2: Push (git-tabanlı; scheduler scripts/** değişti → auto-redeploy tetiklenir)**

```bash
git push origin main
```
Not: scheduler servisi (`benseno-tasarim-sistemi`) `scripts/**` izliyor → yeni script + cron ile otomatik yeniden derlenir/deploy olur.

- [ ] **Step 3: Scheduler yeniden başladı mı**

Run: `railway logs --service benseno-tasarim-sistemi 2>&1 | grep -iE "cron job'lar kuruldu|başlat" | tail -3`
Expected: `[scheduler] cron job'lar kuruldu` (yeni cron dahil).

- [ ] **Step 4: Canlı --dry doğrulama**

Run: `railway run --service benseno-api node scripts/firma-brifing.js --dry`
Expected: olgu özeti (sinyaller + finans) + doğru yönetici hedef listesi; hata yok. (Gerçek Opus brifingi bir sonraki Pazartesi 08:00'de gider.)

---

## Self-Review

**Spec kapsama:**
- Pazartesi 08:00 cron → Task 2 ✓
- Opus sentezi (`claude-opus-4-7`) → Task 1 opusBrifing ✓
- Deterministik fallback → Task 1 fallbackMetin (+ Step 3 testi) ✓
- DRY: sinyalleriHesapla + bnsFinansOzet → Task 1 brifingOlgulari ✓
- Yönetici+GM DM + tip_firma_sinyal toggle → Task 1 main ✓
- Finans yalnız tamamlanandan (sensitive) → Task 1 ✓
- --dry + hata dayanıklılığı (fallback) → Task 1 + Step 3/4 ✓
- Migration yok → not edildi ✓

**Placeholder taraması:** yok. **Çift-link riski** Step 2'de giderildi (fallbackMetin link satırı çıkarıldı; link main'de eklenir).

**Tip tutarlılığı:** `brifingOlgulari`/`fallbackMetin` imzaları Task 1↔testler tutarlı; `sinyalleriHesapla`/`yoneticiMi`/`bnsFinansOzet` mevcut export'lardan (P3.3a/b + P3.1).
