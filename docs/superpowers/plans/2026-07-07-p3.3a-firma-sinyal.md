# P3.3a — Proaktif Firma Sinyal Motoru Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Firma-seviyesi risk sinyallerini (kapasite, geciken, marka-risk, kişi kalite-düşüşü) günde 2 kez tarayıp yöneticilere kendiliğinden Slack DM + dashboard bildirimi olarak push eden deterministik bir motor kurmak.

**Architecture:** Saf sinyal eşikleri `dashboard/app/calc.js`'te (CI formül kilidi). Yeni cron script'i `scripts/firma-sinyal.js` embedded snapshot'tan primitifleri çıkarır, calc fonksiyonlarını çağırır, dinamik yönetici listesine dedup + sessiz-saat + toggle disipliniyle push eder. `ody-icgoru.js` kardeş desenini birebir izler.

**Tech Stack:** Node.js, node-cron (scheduler.js), Postgres (server/db pool), Slack Web API (rapor-lib post/token), calc.js saf formüller.

---

## Referans dosyalar (uygulayıcı önce okusun)

- `scripts/ody-icgoru.js` — bu script'in birebir kardeşi (prefs map, inQuiet, INSERT+post, --dry, dedup).
- `scripts/rapor-lib.js` — `token()`, `post(tok, uid, text)`, `fetchEmbedded()`, `GORKEM='U030C48PL23'`, `DASHBOARD_URL` export'ları.
- `dashboard/app/calc.js` — `var`-style fonksiyon + alttaki `module.exports` listesi deseni. `bnsCapPct`, `bnsGecikmeH`, `BNS_H` mevcut.
- `scripts/formula-test.js` — `t(name, got, want)` test deseni; `const C = require('../dashboard/app/calc.js')`.
- `server/migrations/0014_ody_akil.sql` — migration deseni. Runner: `server/scripts/migrate.js` (boot'ta auto-migrate).
- `server/api.js` `notify_prefs` GET (~728) + POST (~739 INSERT) — toggle kolonları.

**Kritik kural:** `require('../dashboard/app/calc')` yalnız `scripts/` içinde güvenli (cron süreci tüm repo'ya erişir). Bu bağımlılık **asla `server/` içine taşınmaz** (benseno-api Docker imajı yalnız `server/` içerir → MODULE_NOT_FOUND).

---

## Task 1: Migration + notify_prefs toggle kolonu

**Files:**
- Create: `server/migrations/0015_firma_sinyal.sql`
- Modify: `server/api.js` (notify_prefs GET döndürme + POST INSERT — `tip_firma_sinyal` ekle)

- [ ] **Step 1: Migration dosyasını yaz**

`server/migrations/0015_firma_sinyal.sql`:
```sql
-- P3.3a: firma-seviyesi proaktif sinyal push toggle'ı (yönetici kapatabilir)
ALTER TABLE notify_prefs ADD COLUMN IF NOT EXISTS tip_firma_sinyal BOOLEAN NOT NULL DEFAULT true;
```

- [ ] **Step 2: api.js notify_prefs POST INSERT'ine kolonu ekle**

`server/api.js` içinde `INSERT INTO notify_prefs (...)` bloğunu bul (mevcut kolonlar: `ogle_dijest, tip_termin, tip_atama, tip_bloke, sessiz_bas, sessiz_bit, ody_icgoru`). `tip_firma_sinyal`'i hem kolon listesine hem VALUES/UPDATE SET kısmına, diğer boolean toggle'larla aynı şekilde ekle. Body'den okurken varsayılan `true`:
```js
// body parse kısmında diğer toggle'ların yanına:
const tipFirmaSinyal = body.tip_firma_sinyal !== false; // varsayılan açık
```
INSERT ... ON CONFLICT UPDATE bloğuna `tip_firma_sinyal` kolonunu ekle (mevcut `ody_icgoru` kolonunun tam olarak nasıl işlendiğini kopyala).

- [ ] **Step 3: api.js notify_prefs GET yanıtına kolonu ekle**

GET `notify_prefs` `SELECT *` kullanıyorsa kolon otomatik döner — doğrula. Açık kolon listesi varsa `tip_firma_sinyal` ekle.

- [ ] **Step 4: Sözdizimi kontrolü**

Run: `node --check server/api.js`
Expected: çıktı yok (başarılı).

- [ ] **Step 5: Commit**

```bash
git add server/migrations/0015_firma_sinyal.sql server/api.js
git commit -m "feat(p3.3a): notify_prefs.tip_firma_sinyal toggle + migration 0015"
```

---

## Task 2: calc.js saf sinyal fonksiyonları (TDD)

**Files:**
- Modify: `dashboard/app/calc.js` (4 yeni fonksiyon + export listesi)
- Modify: `scripts/formula-test.js` (birim testler)

Sinyal nesnesi sözleşmesi (tüm fonksiyonlar 0..n adet döndürür, dizi olarak):
```
{ tip, aciliyet, key, text }
```
- `tip`: 'firma_kapasite' | 'firma_geciken' | 'marka_risk' | 'kisi_kalite'
- `aciliyet`: 'acil' | 'normal'
- `key`: dedup anahtarı — firma sinyalleri için `null`, marka için marka adı, kişi için kişi adı.
- `text`: kullanıcıya gösterilecek mesaj.

- [ ] **Step 1: Başarısız testleri yaz (formula-test.js sonuna)**

`scripts/formula-test.js` içinde son `t(...)` çağrılarından sonra, `console.log` özet bloğundan ÖNCE ekle:
```js
// ── P3.3a firma sinyalleri ──
// Kapasite: eşik %85
t('kapasite %85 tam → sinyal yok', C.bnsSinyalKapasite(85).length, 0);
t('kapasite %86 → 1 sinyal',       C.bnsSinyalKapasite(86).length, 1);
t('kapasite sinyal tipi',          C.bnsSinyalKapasite(90)[0].tip, 'firma_kapasite');

// Geciken: eşik 5 (deadline < now, durum aktif)
const NOW = new Date('2026-07-07T12:00:00+03:00').getTime();
const gec = (n) => ({ no: n, marka: 'M' + n, deadline: NOW - 3600000, durum: 'devam' });
t('geciken 5 → sinyal yok', C.bnsSinyalGeciken([gec(1),gec(2),gec(3),gec(4),gec(5)], NOW).length, 0);
t('geciken 6 → 1 sinyal',   C.bnsSinyalGeciken([gec(1),gec(2),gec(3),gec(4),gec(5),gec(6)], NOW).length, 1);
t('geciken: tamamlandi hariç', C.bnsSinyalGeciken(
   [gec(1),gec(2),gec(3),gec(4),gec(5),{ no:6, marka:'X', deadline: NOW-3600000, durum:'tamamlandi' }], NOW).length, 0);
t('geciken: gelecek deadline hariç', C.bnsSinyalGeciken(
   [gec(1),gec(2),gec(3),gec(4),gec(5),{ no:6, marka:'X', deadline: NOW+3600000, durum:'devam' }], NOW).length, 0);

// Marka-risk: risk_seviye='yuksek' VEYA thread_ton gergin/acil
t('marka risk_seviye yuksek → sinyal', C.bnsSinyalMarkaRisk(
   [{ marka:'Acme', thread_ton:null }], { Acme:'yuksek' }).length, 1);
t('marka risk_seviye orta → yok', C.bnsSinyalMarkaRisk(
   [{ marka:'Acme', thread_ton:null }], { Acme:'orta' }).length, 0);
t('marka thread_ton gergin → sinyal', C.bnsSinyalMarkaRisk(
   [{ marka:'Beta', thread_ton:'gergin' }], {}).length, 1);
t('marka: aynı marka tek sinyal', C.bnsSinyalMarkaRisk(
   [{ marka:'Beta', thread_ton:'gergin' }, { marka:'Beta', thread_ton:'acil' }], {}).length, 1);
t('marka key = marka adı', C.bnsSinyalMarkaRisk([{ marka:'Beta', thread_ton:'acil' }], {})[0].key, 'Beta');

// Kişi kalite: son5 ort, önceki5 ort; ≥1.0 düşüş VE son<4.0, en az 10 iş
const kisi = (ad, ratings) => ({ id:'U'+ad, ad, ratings });
// oldest→newest: önceki5=[5,5,5,5,5]=5.0, son5=[3,3,3,4,4]=3.4 → düşüş 1.6, son<4 → sinyal
t('kişi ≥1.0 düşüş & son<4 → sinyal', C.bnsSinyalKisiKalite(
   [kisi('A', [5,5,5,5,5, 3,3,3,4,4])]).length, 1);
// önceki5=4.0, son5=3.8 → düşüş 0.2 → yok
t('kişi <1.0 düşüş → yok', C.bnsSinyalKisiKalite(
   [kisi('B', [4,4,4,4,4, 4,4,4,3,4])]).length, 0);
// düşüş büyük ama son5=[4,4,4,5,5]=4.4 ≥4.0 → yok
t('kişi son≥4.0 → yok', C.bnsSinyalKisiKalite(
   [kisi('C', [5,5,5,5,5, 4,4,4,5,5])]).length, 0);
// yalnız 8 iş → yeterli veri yok → yok
t('kişi <10 iş → yok', C.bnsSinyalKisiKalite(
   [kisi('D', [5,5,5,5, 2,2,2,2])]).length, 0);
```

- [ ] **Step 2: Testi çalıştır, başarısız olduğunu gör**

Run: `node scripts/formula-test.js`
Expected: `C.bnsSinyalKapasite is not a function` benzeri hata / FAIL.

- [ ] **Step 3: calc.js'e 4 fonksiyonu ekle (module.exports'tan ÖNCE)**

`dashboard/app/calc.js` içinde, `bnsFinansOzet` fonksiyonundan sonra ve `module.exports` bloğundan önce:
```js
// ── P3.3a firma-seviyesi proaktif sinyaller (deterministik; push motoru scripts/firma-sinyal.js) ──
function bnsSinyalKapasite(capPct) {
  if (capPct == null || capPct <= 85) return [];
  return [{ tip: 'firma_kapasite', aciliyet: 'acil', key: null,
    text: '⚠️ Firma kapasitesi %' + Math.round(capPct) + ' — yük dengelemesi gerekebilir.' }];
}
function bnsSinyalGeciken(briefs, now) {
  var gec = (briefs || []).filter(function (b) {
    return b.deadline != null && b.deadline < now &&
      ['tamamlandi', 'musteride'].indexOf(b.durum) === -1;
  });
  if (gec.length <= 5) return [];
  gec.sort(function (a, b) { return (a.deadline || 0) - (b.deadline || 0); });
  var top = gec[0];
  return [{ tip: 'firma_geciken', aciliyet: 'acil', key: null,
    text: '🔴 ' + gec.length + ' iş gecikmede (>5 eşik). En kritik: #' + top.no + ' ' + (top.marka || '') + '.' }];
}
function bnsSinyalMarkaRisk(briefs, riskMap) {
  var rm = riskMap || {};
  var byMarka = {};
  (briefs || []).forEach(function (b) {
    if (!b.marka) return;
    var yuksek = rm[b.marka] === 'yuksek';
    var kotuTon = ['gergin', 'acil'].indexOf(b.thread_ton) !== -1;
    if (yuksek || kotuTon) {
      if (!byMarka[b.marka]) byMarka[b.marka] = rm[b.marka] || b.thread_ton;
    }
  });
  return Object.keys(byMarka).map(function (marka) {
    return { tip: 'marka_risk', aciliyet: 'acil', key: marka,
      text: '📉 ' + marka + ' müşteri-risk sinyali: ' + byMarka[marka] + '. İlişkiye göz at.' };
  });
}
function bnsSinyalKisiKalite(kisiler) {
  var out = [];
  (kisiler || []).forEach(function (k) {
    var r = k.ratings || [];
    if (r.length < 10) return; // yeterli veri yok
    var son5 = r.slice(-5), onceki5 = r.slice(-10, -5);
    var avg = function (a) { return a.reduce(function (s, x) { return s + x; }, 0) / a.length; };
    var sonAvg = avg(son5), onckAvg = avg(onceki5);
    if (onckAvg - sonAvg >= 1.0 && sonAvg < 4.0) {
      out.push({ tip: 'kisi_kalite', aciliyet: 'normal', key: k.ad,
        text: '📊 ' + k.ad + ' son işlerde puan düşüşü (' +
          (Math.round(onckAvg * 10) / 10) + '→' + (Math.round(sonAvg * 10) / 10) +
          '). Kısa bir mentörlük görüşmesi iyi gelebilir.' });
    }
  });
  return out;
}
```

- [ ] **Step 4: Export listesine 4 fonksiyonu ekle**

`module.exports = { ... }` listesinin sonuna `bnsKarMarj, bnsFinansOzet`'ten sonra ekle:
```js
bnsSinyalKapasite, bnsSinyalGeciken, bnsSinyalMarkaRisk, bnsSinyalKisiKalite
```

- [ ] **Step 5: Testi çalıştır, geçtiğini gör**

Run: `node scripts/formula-test.js`
Expected: tüm yeni testler `✅`, `FAIL=0`, exit 0.

- [ ] **Step 6: Commit**

```bash
git add dashboard/app/calc.js scripts/formula-test.js
git commit -m "feat(p3.3a): 4 firma sinyal fonksiyonu + birim testleri (calc.js)"
```

---

## Task 3: Orkestrasyon script'i — scripts/firma-sinyal.js

**Files:**
- Create: `scripts/firma-sinyal.js`

Bu script `ody-icgoru.js`'i birebir izler: embedded al, sinyalleri hesapla, dinamik yönetici listesine dedup + sessiz-saat + toggle ile push.

- [ ] **Step 1: Embedded kullanıcı şeklini doğrula (yönetici alanı: rol mi yetki mi)**

Run:
```bash
BNS_REPORT_LIVE=1 node -e "require('./scripts/rapor-lib').fetchEmbedded().then(d=>{const u=(d.bns_users||[]).find(x=>/^U/.test(x.id));console.log(JSON.stringify(Object.keys(u)));console.log('rol=',u.rol,'yetki=',u.yetki);}).catch(e=>console.error(e.message))"
```
Expected: kullanıcı nesnesinin anahtarları listelenir. Yönetici alanı `rol==='yonetici'` mi `yetki==='yonetici'` mi NOT ET. Aşağıdaki kodda `MGR_FIELD` sabitini buna göre ayarla. Ayrıca embedded'da firma kapasite yüzdesinin nerede olduğunu (ör. `d.bns_stats.capacity_pct` veya benzeri) NOT ET; Step 2'de `capPct` kaynağı buna göre bağlanır. Marka `risk_seviye` embedded'da yoksa Step 2'deki `brand_daily` sorgusu kullanılır.

- [ ] **Step 2: firma-sinyal.js dosyasını yaz**

`scripts/firma-sinyal.js`:
```js
'use strict';
/**
 * firma-sinyal.js — Firma-seviyesi proaktif sinyaller (P3.3a). Günde 2 kez (09:00/15:00) tarar,
 * eşik ihlallerini yöneticilere Slack DM + dashboard bildirimi olarak push eder.
 * Deterministik (LLM yok). ody-icgoru.js kardeş deseni.
 *   node scripts/firma-sinyal.js         → canlı (BNS_REPORT_LIVE=1)
 *   node scripts/firma-sinyal.js --dry    → DB/DM yok; sinyaller + hedef liste yazdır
 */
const { token, post, fetchEmbedded, GORKEM, DASHBOARD_URL } = require('./rapor-lib');
const { pool } = require('../server/db');
const C = require('../dashboard/app/calc.js');

const DRY = process.argv.includes('--dry');
const MGR_FIELD = 'rol'; // Step 1'de doğrula: 'rol' veya 'yetki'

// İstanbul saatine göre sessiz-saat (ody-icgoru ile aynı mantık)
function inQuiet(p, trHour) {
  const bas = (p && p.sessiz_bas != null) ? p.sessiz_bas : 19;
  const bit = (p && p.sessiz_bit != null) ? p.sessiz_bit : 8;
  return bas > bit ? (trHour >= bas || trHour < bit) : (trHour >= bas && trHour < bit);
}

// Embedded'dan sinyal girdilerini çıkar → tüm sinyalleri topla
async function sinyalleriHesapla(d, now) {
  const briefs = d.bns_briefs || [];
  const out = [];

  // 1. Kapasite: embedded firma stat'ından yüzde (Step 1'de kaynağı doğrula)
  const capPct = (d.bns_stats && d.bns_stats.capacity_pct != null)
    ? d.bns_stats.capacity_pct
    : C.bnsCapPct(d.bns_stats || {});
  out.push(...C.bnsSinyalKapasite(capPct));

  // 2. Geciken
  out.push(...C.bnsSinyalGeciken(briefs, now));

  // 3. Marka-risk: brand_daily.risk_seviye + thread_ton
  let riskMap = {};
  if (!DRY) {
    try {
      const r = await pool.query(
        `SELECT DISTINCT ON (marka) marka, risk_seviye FROM brand_daily
          WHERE risk_seviye IS NOT NULL ORDER BY marka, gun DESC`);
      r.rows.forEach((x) => { if (x.marka) riskMap[x.marka] = x.risk_seviye; });
    } catch (e) { console.error('[firma-sinyal] risk_seviye sorgu hatası:', e.message); }
  }
  out.push(...C.bnsSinyalMarkaRisk(briefs, riskMap));

  // 4. Kişi kalite: kişi başına puanlı işleri tarih sırasıyla topla
  const byKisi = {};
  briefs.forEach((b) => {
    if (b.rating == null || !b.bitis) return;
    (b.workers || []).concat(b.leads || []).forEach((p) => {
      if (!p || !/^U/.test(p.id)) return;
      (byKisi[p.id] = byKisi[p.id] || { id: p.id, ad: (p.name || '').split(' ')[0] || p.name, rows: [] })
        .rows.push({ t: b.bitis, r: b.rating });
    });
  });
  const kisiler = Object.values(byKisi).map((k) => ({
    id: k.id, ad: k.ad,
    ratings: k.rows.sort((a, b) => a.t - b.t).map((x) => x.r), // oldest→newest
  }));
  out.push(...C.bnsSinyalKisiKalite(kisiler));

  return out;
}

// Bir sinyalin bugün bu tip+key için zaten push edilip edilmediği (İstanbul günü)
async function dahaOnceMi(uid, tip, key) {
  const r = await pool.query(
    `SELECT 1 FROM notifications
      WHERE user_id=$1 AND tip=$2
        AND (marka=$3 OR ($3 IS NULL AND marka IS NULL))
        AND (created_at AT TIME ZONE 'Europe/Istanbul')::date = (now() AT TIME ZONE 'Europe/Istanbul')::date
      LIMIT 1`, [uid, tip, key]);
  return r.rowCount > 0;
}

async function main() {
  const tok = token();
  if (!tok && !DRY) { console.error('SLACK token yok'); process.exit(1); }
  const live = process.env.BNS_REPORT_LIVE === '1';
  const now = Date.now();
  const d = await fetchEmbedded();

  const sinyaller = await sinyalleriHesapla(d, now);

  // Dinamik yönetici listesi: rol='yonetici' + GORKEM
  const users = (d.bns_users || []).filter((u) => /^U/.test(u.id));
  const yoneticiler = users.filter((u) => u[MGR_FIELD] === 'yonetici' || u.id === GORKEM);
  const ids = Array.from(new Set(yoneticiler.map((u) => u.id).concat(GORKEM)));

  if (DRY) {
    console.log(`firma-sinyal DRY — ${sinyaller.length} sinyal, ${ids.length} yönetici hedefi:`);
    console.log('  hedefler:', ids.join(', '));
    sinyaller.forEach((s) => console.log(`  [${s.tip}/${s.key || 'firma'}] ${s.text}`));
    await pool.end();
    return;
  }

  // Tercihler + sessiz-saat
  const prefs = new Map((await pool.query(
    `SELECT user_id, tip_firma_sinyal, sessiz_bas, sessiz_bit FROM notify_prefs`)).rows.map((r) => [r.user_id, r]));
  const trHour = parseInt(new Date().toLocaleString('en-US', { timeZone: 'Europe/Istanbul', hour12: false, hour: '2-digit' }), 10);

  let sent = 0;
  for (const s of sinyaller) {
    for (const uid of ids) {
      const p = prefs.get(uid);
      if (p && p.tip_firma_sinyal === false) continue;      // master toggle kapalı
      if (await dahaOnceMi(uid, s.tip, s.key)) continue;    // günlük dedup

      const dmIzin = live && !inQuiet(p, trHour);
      let dmAtildi = false;
      if (dmIzin) dmAtildi = await post(tok, uid, `${s.text}\n🔗 ${DASHBOARD_URL}`);
      try {
        await pool.query(
          `INSERT INTO notifications (user_id, tip, aciliyet, text, brief_id, dijest_at, slack_at, marka)
           VALUES ($1,$2,$3,$4,NULL, now(), $5, $6)`,
          [uid, s.tip, s.aciliyet, s.text, dmAtildi ? new Date() : null, s.key]);
        sent++;
      } catch (e) {
        if (e.code === '23505') { console.error('[firma-sinyal] çift kayıt (23505):', uid, s.tip); continue; }
        throw e;
      }
    }
  }
  console.log(`firma-sinyal ${live ? 'CANLI' : 'TEST'} — ${sinyaller.length} sinyal, ${sent} bildirim`);
  await pool.end();
}

if (require.main === module) main().catch((e) => { console.error('firma-sinyal hata:', e.message); process.exit(1); });
module.exports = { sinyalleriHesapla };
```

- [ ] **Step 3: Sözdizimi kontrolü**

Run: `node --check scripts/firma-sinyal.js`
Expected: çıktı yok.

- [ ] **Step 4: --dry ile canlı snapshot üzerinde doğrula**

Run: `BNS_REPORT_LIVE=1 node scripts/firma-sinyal.js --dry`
Expected: sinyal listesi + yönetici hedef listesi yazdırılır, DB/DM'e dokunulmaz, hata yok. Yönetici listesi doğru görünmüyorsa `MGR_FIELD`'i düzelt.

- [ ] **Step 5: Commit**

```bash
git add scripts/firma-sinyal.js
git commit -m "feat(p3.3a): firma-sinyal orkestrasyon script'i (dedup + sessiz-saat + toggle)"
```

---

## Task 4: Cron wrapper + scheduler kaydı

**Files:**
- Create: `scripts/run-firma-sinyal.sh`
- Modify: `scripts/scheduler.js`

- [ ] **Step 1: Wrapper script'i yaz (mevcut run-*.sh desenini kopyala)**

Önce mevcut bir örneği oku: `cat scripts/run-ody-icgoru.sh`. Aynı yapıyı `firma-sinyal.js` için `scripts/run-firma-sinyal.sh` olarak oluştur (script adı dışında aynı). Çalıştırılabilir yap:
```bash
chmod +x scripts/run-firma-sinyal.sh
```

- [ ] **Step 2: scheduler.js'e iki cron ekle**

`scripts/scheduler.js` içinde mevcut `cron.schedule('15 8 * * 1-5', () => run('run-ody-icgoru.sh'), opts);` satırının yanına (aynı `if (proaktif açık)` bloğu içindeyse oraya) ekle:
```js
// P3.3a: firma-seviyesi proaktif sinyaller (09:00 + 15:00, hafta içi)
cron.schedule('0 9 * * 1-5',  () => run('run-firma-sinyal.sh'), opts);
cron.schedule('0 15 * * 1-5', () => run('run-firma-sinyal.sh'), opts);
```

- [ ] **Step 3: Sözdizimi kontrolü**

Run: `node --check scripts/scheduler.js`
Expected: çıktı yok.

- [ ] **Step 4: Commit**

```bash
git add scripts/run-firma-sinyal.sh scripts/scheduler.js
git commit -m "feat(p3.3a): firma-sinyal cron (09:00+15:00) + run wrapper"
```

---

## Task 5: Dashboard ayar toggle'ı (tip_firma_sinyal)

**Files:**
- Modify: `notify_prefs` toggle'larının render edildiği ekran (ody_icgoru toggle'ını ara: `grep -rn "ody_icgoru" dashboard/app`)

- [ ] **Step 1: Mevcut toggle render yerini bul**

Run: `grep -rn "ody_icgoru\|tip_termin" dashboard/app`
Expected: ayar ekranındaki toggle listesi bulunur (ör. Ayarlar/Bildirim tercihleri bileşeni).

- [ ] **Step 2: tip_firma_sinyal toggle'ını ekle**

Mevcut `ody_icgoru` toggle'ının hemen yanına, aynı bileşen/desenle bir toggle ekle. Yalnız yöneticiye anlamlı olduğundan, etiket: **"Firma risk sinyalleri (yönetici)"**. State + POST payload'ına `tip_firma_sinyal` alanını ekle (mevcut toggle'lar payload'a nasıl giriyorsa aynı).

- [ ] **Step 3: JSX derlenebilirlik**

Run: `bash scripts/ci-check.sh` (② esbuild parse adımını izle)
Expected: `✅ tüm .jsx derlendi`.

- [ ] **Step 4: Commit**

```bash
git add dashboard/app
git commit -m "feat(p3.3a): ayarlar — firma sinyal toggle'ı (tip_firma_sinyal)"
```

---

## Task 6: Entegrasyon — CI + build + deploy + doğrulama

**Files:** — (yalnız çalıştırma)

- [ ] **Step 1: Tam CI kapısı**

Run: `bash scripts/ci-check.sh`
Expected: `🟢 CI KAPISI GEÇTİ` (formül testleri yeni sinyalleri içerir; JSX derlenir; tek-tanım güvencesi geçer).

- [ ] **Step 2: Dashboard bundle build (Task 5 JSX değişikliği için)**

Run: `bash scripts/build-dashboard.sh`
Expected: bundle üretildi; `app/` + `index.html` EMBEDDED senkron.

- [ ] **Step 3: API deploy (migration 0015 boot'ta auto-migrate ile uygulanır)**

Run: `bash scripts/deploy.sh api`
Not: deploy 2dk'yı aşabilir; timeout olursa `git push origin main` ile remote'u senkronla ve health bekle.

- [ ] **Step 4: Migration + health doğrula**

```bash
railway logs --service benseno-api 2>&1 | grep -iE "0015|uygulandı|dinleniyor" | tail -5
curl -s -o /dev/null -w "%{http_code}\n" https://benseno-api-production.up.railway.app/health
```
Expected: `✓ uygulandı: 0015_firma_sinyal.sql`, health `200`.

- [ ] **Step 5: Dashboard deploy (Task 5 UI değişikliği için)**

Run: `bash scripts/deploy.sh dashboard`
Expected: Pages yayınlandı.

- [ ] **Step 6: Canlı --dry son doğrulama**

Run: `BNS_REPORT_LIVE=1 node scripts/firma-sinyal.js --dry`
Expected: gerçek snapshot'ta sinyaller + doğru yönetici hedef listesi; hata yok. (scheduler cron'ları bir sonraki 09:00/15:00'te canlı push yapacak.)

---

## Self-Review (yazımdan sonra kontrol edildi)

**Spec kapsama:**
- Kapasite/geciken/marka-risk/kişi-kalite 4 sinyali → Task 2 ✓
- Push motoru (dedup + sessiz-saat + toggle + --dry) → Task 3 ✓
- notify_prefs.tip_firma_sinyal → Task 1 + Task 5 (UI) ✓
- Migration 0015 auto-migrate → Task 1 + Task 6 ✓
- Cron 09:00/15:00, dinamik yönetici + GM → Task 3 + Task 4 ✓
- calc.js tek-tanım + CI testleri → Task 2 + Task 6 ✓
- dashboard/calc bağımlılığı yalnız scripts/'te → referans notu + Task 3 ✓

**Belirsizlikler bilinçli ele alındı:** Yönetici alanı (`rol`/`yetki`) ve firma kapasite kaynağı Task 3 Step 1'de doğrulanır (tahmin edilmez). Marka `risk_seviye` embedded'da yoksa `brand_daily` sorgusuyla alınır.

**Tip tutarlılığı:** Sinyal nesnesi `{tip, aciliyet, key, text}` tüm fonksiyonlarda ve script INSERT'inde (`key`→`marka` kolonu) tutarlı.
