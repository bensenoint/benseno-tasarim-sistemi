# P3.4c — Self-Servis Termin/Hatırlat Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Atanan (worker) kendi işinde termin-uzat (öneri varken) ve hatırlat kullanabilsin; UI kuralı calc.js'te, hatırlat server yetkisi api.js'te gevşetilir.

**Architecture:** İki satır kural değişikliği (`bnsBriefActionPerms`) + bir SQL filtre değişikliği (`/remind` lead→assignee) + perms birim testleri. `termin_oneri_ms` ön-koşulu ve 10dk spam freni korunur.

**Tech Stack:** calc.js, formula-test.js, Express (server/api.js).

---

## Task 1: calc.js perms kuralı (TDD)

**Files:**
- Modify: `dashboard/app/calc.js:279-280`
- Modify: `scripts/formula-test.js`

- [ ] **Step 1: Başarısız testler (özet console.log'dan ÖNCE)**

```js
// ── P3.4c self-servis termin/hatırlat (perms) ──
const pB = { leads: [{id:'L1'}], workers: [{id:'W1'}], created_by: 'C1', durum: 'calisiliyor', termin_oneri_ms: 3600000 };
const pB0 = { ...pB, termin_oneri_ms: null };
t('perms: worker termin (öneri var)', C.bnsBriefActionPerms(pB, {id:'W1'}).termin, true);
t('perms: worker termin (öneri yok)', C.bnsBriefActionPerms(pB0, {id:'W1'}).termin, false);
t('perms: worker hatirlat', C.bnsBriefActionPerms(pB, {id:'W1'}).hatirlat, true);
t('perms: açan hatirlat', C.bnsBriefActionPerms(pB, {id:'C1'}).hatirlat, true);
t('perms: yabancı termin', C.bnsBriefActionPerms(pB, {id:'X9'}).termin, false);
t('perms: yabancı hatirlat', C.bnsBriefActionPerms(pB, {id:'X9'}).hatirlat, false);
t('perms: lead hatirlat (regresyon)', C.bnsBriefActionPerms(pB, {id:'L1'}).hatirlat, true);
```

- [ ] **Step 2: FAIL gör** — `node scripts/formula-test.js` (worker termin/hatirlat + açan hatirlat testleri ❌).

- [ ] **Step 3: Kuralı değiştir (calc.js:279-280)**

Eski:
```js
  out.termin = !!b.termin_oneri_ms && (isLead || isCreator || isMgr);
  out.hatirlat = isLead || isMgr;
```
Yeni:
```js
  // P3.4c (Ç6): atanan (worker) kendi işinde termin uzatabilir/hatırlatabilir — lead bağımlılığı kalkar.
  out.termin = !!b.termin_oneri_ms && (isAssignee || isCreator || isMgr);
  out.hatirlat = isAssignee || isCreator || isMgr;
```

- [ ] **Step 4: PASS gör** — tümü ✅, FAIL=0.

- [ ] **Step 5: Commit**

```bash
git add dashboard/app/calc.js scripts/formula-test.js
git commit -m "feat(p3.4c): perms — atanan termin-uzat/hatırlat (Ç6) + 7 test

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

## Task 2: server /remind yetkisi

**Files:**
- Modify: `server/api.js:811-815`

- [ ] **Step 1: lead filtresini assignee'ye genişlet**

Eski (satır 811-814 civarı):
```js
    // Yetki: yalnız yönetici, işin lead'i veya işi açan hatırlatabilir (UI perms ile aynı).
    const isLead = (await pool.query(`SELECT 1 FROM brief_assignees WHERE brief_id=$1 AND user_id=$2 AND role='lead' LIMIT 1`, [id, actor])).rowCount > 0;
    if (!isMgr && !isLead && bi.created_by !== actor) return res.status(403).json({ error: 'yetki yok' });
```
Yeni:
```js
    // Yetki (P3.4c): yönetici, işin HERHANGİ bir atananı (lead/worker) veya işi açan hatırlatabilir (UI perms ile aynı).
    const isAssignee = (await pool.query(`SELECT 1 FROM brief_assignees WHERE brief_id=$1 AND user_id=$2 LIMIT 1`, [id, actor])).rowCount > 0;
    if (!isMgr && !isAssignee && bi.created_by !== actor) return res.status(403).json({ error: 'yetki yok' });
```
(`isMgr` sorgusu ve 10dk dedup aynen kalır.)

- [ ] **Step 2: Sözdizimi** — `node --check server/api.js`.

- [ ] **Step 3: Commit**

```bash
git add server/api.js
git commit -m "feat(p3.4c): /remind — atanan (worker) hatırlatabilir; 10dk dedup korunur

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

## Task 3: CI + build + iki servis deploy + doğrulama

- [ ] **Step 1: CI** — `bash scripts/ci-check.sh` → `🟢 CI KAPISI GEÇTİ`.
- [ ] **Step 2: Dashboard build** — `bash scripts/build-dashboard.sh` (calc bundle'a girsin).
- [ ] **Step 3: Build commit + API deploy + dashboard deploy + push**

```bash
git add -A && git commit -m "build(p3.4c): bundle senkron (perms)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
bash scripts/deploy.sh api
bash scripts/deploy.sh dashboard
git push origin main
```
- [ ] **Step 4: Doğrulama** — health 200; `railway logs --service benseno-api | grep dinleniyor`.

## Self-Review

- Spec kapsama: calc iki kural ✓ Task 1; /remind assignee ✓ Task 2; termin server değişmez ✓ (görev yok — bilinçli); testler (worker/açan/yabancı/öneri-yok/lead regresyon) ✓; iki servis deploy ✓ Task 3.
- Tip tutarlılığı: `isAssignee` calc'ta zaten tanımlı (satır 271); server'da yeni const adı çakışmıyor.
- Placeholder yok.
