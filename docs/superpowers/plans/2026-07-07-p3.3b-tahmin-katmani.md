# P3.3b — Tahmin Katmanı Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Gecikme öngörüsü + burnout projeksiyonu sinyallerini (P3.3a motoruna) ve kişi-performans metrik hesaplama katmanını (P3.4 temeli) gözlemlenen veriden üretmek; at-risk brief'lerde dashboard rozeti göstermek.

**Architecture:** Saf calc.js fonksiyonları (baseline, gecikme öngörüsü, burnout, kişi-performans + 2 sinyal sarmalayıcı). `firma-sinyal.js`'e 2 yeni sinyal (`gecikme_ongoru`, `burnout`) eklenir — P3.3a dedup/sessiz-saat/toggle motorunu aynen kullanır. Dashboard BriefDrawer'a "⏳ öngörülen gecikme" rozeti (atanan+yönetici).

**Tech Stack:** Node.js, calc.js saf formüller, firma-sinyal.js (cron), React UMD/JSX dashboard.

---

## Referans / kısıtlar

- `tahmini_sure_h` **%0 dolu** → per-iş tahmin YOK. Her şey gözlemlenen veriden (döngü, revize, deadline, kapasite).
- **Migration yok** — yeni sinyaller mevcut `notifications` + `tip_firma_sinyal` (P3.3a) toggle'ını kullanır.
- **completed brief'lerde `dept` YOK** (yalnız `marka`) → baseline **marka** üzerinden.
- `bnsCycleSure(events, nowMs, fallback)` → `{cycles, sonH, toplamH}`. Açık iş için `toplamH` = elapsed saat. events = `b.durum_olaylari` ({ts, durum}).
- calc.js `var`-style + alttaki `module.exports` listesi. formula-test.js `t(name, got, want)`.
- `require('../dashboard/app/calc')` yalnız scripts/'te güvenli (server/'a taşıma).

---

## Task 1: calc.js tahmin + metrik fonksiyonları (TDD)

**Files:**
- Modify: `dashboard/app/calc.js` (6 fonksiyon + export)
- Modify: `scripts/formula-test.js` (birim testler)

- [ ] **Step 1: Başarısız testleri yaz (formula-test.js, özet console.log'dan ÖNCE)**

```js
// ── P3.3b tahmin katmanı ──
const H2 = C.BNS_H;
// baseline: marka medyan döngüsü, <3 örnek → null
const comp = (marka, sureH) => ({ marka, sureH });
t('baseline <3 örnek → null', C.bnsBaselineCycle([comp('A',10),comp('A',20)], 'A'), null);
t('baseline 3 örnek medyan', C.bnsBaselineCycle([comp('A',10),comp('A',20),comp('A',30)], 'A'), 20);
t('baseline farklı marka sayılmaz', C.bnsBaselineCycle([comp('A',10),comp('A',20),comp('A',30),comp('B',99)], 'A'), 20);

// gecikme öngörüsü
const NOWG = new Date('2026-07-07T12:00:00+03:00').getTime();
const dlIn = (h) => NOWG + h*H2;
// projMiss: baseline 40h, elapsed 10h → kalan iş 30h > kalan zaman 20h → risk
t('gecikme projMiss → risk', C.bnsGecikmeOngoru({ deadline: dlIn(20), durum:'calisiliyor', rev_ic:0, rev_musteri:0, elapsedH:10 }, 40, NOWG).risk, true);
t('gecikme projMiss sebep', C.bnsGecikmeOngoru({ deadline: dlIn(20), durum:'calisiliyor', rev_ic:0, rev_musteri:0, elapsedH:10 }, 40, NOWG).sebep, 'projeksiyon');
// bol zaman → risk yok
t('gecikme bol zaman → risk yok', C.bnsGecikmeOngoru({ deadline: dlIn(100), durum:'calisiliyor', rev_ic:0, rev_musteri:0, elapsedH:10 }, 40, NOWG).risk, false);
// davranışsal: revize≥2 + erken durum + <48h, baseline null
t('gecikme davranışsal → risk', C.bnsGecikmeOngoru({ deadline: dlIn(24), durum:'revizyon', rev_ic:1, rev_musteri:1, elapsedH:5 }, null, NOWG).sebep, 'davranissal');
// teslim edilmiş durum → risk yok
t('gecikme incelemede → risk yok', C.bnsGecikmeOngoru({ deadline: dlIn(1), durum:'incelemede', rev_ic:3, rev_musteri:0, elapsedH:99 }, 40, NOWG).risk, false);

// burnout
t('burnout %119 → asiri false', C.bnsBurnout(5, 5).asiri === false && C.bnsBurnout(5,5).pct === 100, true);
t('burnout %120 → asiri true', C.bnsBurnout(6, 5).asiri, true);
t('burnout capLimit 0 → %0', C.bnsBurnout(3, 0).pct, 0);

// kişi-performans
const cb = (o) => Object.assign({ sureH:10, deadline: 1000, bitis: 900, rev_ic:0, rev_musteri:0, rating:5, marka:'A' }, o);
const perf = C.bnsKisiPerformans([
  cb({ sureH:10, bitis:900, deadline:1000, rev_ic:1, rev_musteri:0, rating:5, marka:'A' }),
  cb({ sureH:20, bitis:1200, deadline:1000, rev_ic:1, rev_musteri:2, rating:3, marka:'B' }),
], NOWG);
t('perf tamamlanan', perf.tamamlanan, 2);
t('perf ortDonguH', perf.ortDonguH, 15);
t('perf zamanindaPct (1/2)', perf.zamanindaPct, 50);
t('perf ortRevize', perf.ortRevize, 2);
t('perf ortPuan', perf.ortPuan, 4);
t('perf tipDagilim', JSON.stringify(perf.tipDagilim), JSON.stringify({ A:1, B:1 }));
t('perf boş girdi', C.bnsKisiPerformans([], NOWG).tamamlanan, 0);

// sinyal sarmalayıcılar
t('sinyal gecikme sayısı+key', (() => { const s = C.bnsSinyalGecikme([{ marka:'Z', no:5 }]); return s.length===1 && s[0].key==='Z' && s[0].tip==='gecikme_ongoru'; })(), true);
t('sinyal burnout sayısı+key', (() => { const s = C.bnsSinyalBurnout([{ ad:'Eda', pct:140 }]); return s.length===1 && s[0].key==='Eda' && s[0].tip==='burnout'; })(), true);
```

- [ ] **Step 2: Testi çalıştır, başarısız gör**

Run: `node scripts/formula-test.js`
Expected: `bnsBaselineCycle is not a function` / FAIL.

- [ ] **Step 3: calc.js'e fonksiyonları ekle (P3.3a sinyalleri sonrası, module.exports ÖNCESİ)**

```js
// ── P3.3b tahmin katmanı (gözlemlenen veriden; tahmini_sure_h kullanılmaz) ──
function bnsBaselineCycle(completed, marka) {
  var arr = (completed || []).filter(function (b) { return b.marka === marka && typeof b.sureH === 'number'; })
    .map(function (b) { return b.sureH; }).sort(function (a, b) { return a - b; });
  if (arr.length < 3) return null;
  var mid = Math.floor(arr.length / 2);
  return arr.length % 2 ? arr[mid] : (arr[mid - 1] + arr[mid]) / 2;
}
function bnsGecikmeOngoru(b, baselineH, now) {
  if (['incelemede', 'musteride', 'tamamlandi'].indexOf(b.durum) !== -1) return { risk: false, sebep: null };
  if (b.deadline == null) return { risk: false, sebep: null };
  var timeLeftH = (b.deadline - now) / BNS_H;
  if (baselineH != null && (baselineH - (b.elapsedH || 0)) > timeLeftH) return { risk: true, sebep: 'projeksiyon' };
  var revize = (b.rev_ic || 0) + (b.rev_musteri || 0);
  if (revize >= 2 && ['yeni', 'calisiliyor', 'revizyon'].indexOf(b.durum) !== -1 && timeLeftH < 48) {
    return { risk: true, sebep: 'davranissal' };
  }
  return { risk: false, sebep: null };
}
function bnsBurnout(upcomingCount, capLimit) {
  var pct = capLimit > 0 ? Math.round((upcomingCount / capLimit) * 100) : 0;
  return { pct: pct, asiri: pct >= 120 };
}
function bnsKisiPerformans(completed, now) {
  var arr = completed || [];
  var n = arr.length;
  if (!n) return { tamamlanan: 0, ortDonguH: null, zamanindaPct: null, ortRevize: null, ortPuan: null, tipDagilim: {}, throughputHaftalik: 0 };
  var r1 = function (x) { return Math.round(x * 10) / 10; };
  var donguler = arr.filter(function (b) { return typeof b.sureH === 'number'; }).map(function (b) { return b.sureH; });
  var ortDonguH = donguler.length ? r1(donguler.reduce(function (a, x) { return a + x; }, 0) / donguler.length) : null;
  var dlOlan = arr.filter(function (b) { return b.deadline != null && b.bitis != null; });
  var zamaninda = dlOlan.filter(function (b) { return b.bitis <= b.deadline; }).length;
  var zamanindaPct = dlOlan.length ? Math.round((zamaninda / dlOlan.length) * 100) : null;
  var ortRevize = r1(arr.reduce(function (a, b) { return a + (b.rev_ic || 0) + (b.rev_musteri || 0); }, 0) / n);
  var puanlar = arr.filter(function (b) { return b.rating != null; }).map(function (b) { return b.rating; });
  var ortPuan = puanlar.length ? r1(puanlar.reduce(function (a, x) { return a + x; }, 0) / puanlar.length) : null;
  var tipDagilim = {};
  arr.forEach(function (b) { if (b.marka) tipDagilim[b.marka] = (tipDagilim[b.marka] || 0) + 1; });
  var dortHaftaOnce = (now || 0) - 28 * 24 * BNS_H;
  var son4 = arr.filter(function (b) { return b.bitis != null && b.bitis >= dortHaftaOnce; }).length;
  return { tamamlanan: n, ortDonguH: ortDonguH, zamanindaPct: zamanindaPct, ortRevize: ortRevize,
    ortPuan: ortPuan, tipDagilim: tipDagilim, throughputHaftalik: r1(son4 / 4) };
}
function bnsSinyalGecikme(atRiskBriefs) {
  return (atRiskBriefs || []).map(function (b) {
    return { tip: 'gecikme_ongoru', aciliyet: 'acil', key: b.marka || null,
      text: '⏳ #' + b.no + ' ' + (b.marka || '') + ' — öngörülen gecikme (deadline yetişmeyebilir).' };
  });
}
function bnsSinyalBurnout(kisiler) {
  return (kisiler || []).map(function (k) {
    return { tip: 'burnout', aciliyet: 'normal', key: k.ad || null,
      text: '🔥 ' + k.ad + ' gelecek 5 günde %' + k.pct + ' yüklü — burnout riski, yük dengelemesi düşün.' };
  });
}
```

- [ ] **Step 4: Export listesine ekle**

`module.exports` sonuna (P3.3a sinyallerinden sonra):
```js
bnsBaselineCycle, bnsGecikmeOngoru, bnsBurnout, bnsKisiPerformans, bnsSinyalGecikme, bnsSinyalBurnout
```

- [ ] **Step 5: Testi çalıştır, geçtiğini gör**

Run: `node scripts/formula-test.js`
Expected: tüm yeni testler `✅`, `FAIL=0`.

- [ ] **Step 6: Commit**

```bash
git add dashboard/app/calc.js scripts/formula-test.js
git commit -m "feat(p3.3b): tahmin + kişi-performans calc fonksiyonları + testler

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: firma-sinyal.js — gecikme + burnout sinyalleri

**Files:**
- Modify: `scripts/firma-sinyal.js` (sinyalleriHesapla içine 2 blok)

- [ ] **Step 1: sinyalleriHesapla'ya gecikme + burnout ekle**

`scripts/firma-sinyal.js` `sinyalleriHesapla` fonksiyonunda, kişi-kalite bloğundan SONRA, `return out;`'tan ÖNCE ekle:
```js
  // 5. Gecikme öngörüsü: her aktif brief için marka baseline + açık döngü elapsed
  const atRisk = [];
  aktif.forEach((b) => {
    const baselineH = C.bnsBaselineCycle(tamamlanan, b.marka);
    const elapsedH = C.bnsCycleSure(b.durum_olaylari || [], now).toplamH || 0;
    const o = C.bnsGecikmeOngoru({ deadline: b.deadline, durum: b.durum, rev_ic: b.rev_ic, rev_musteri: b.rev_musteri, elapsedH }, baselineH, now);
    if (o.risk) atRisk.push(b);
  });
  out.push(...C.bnsSinyalGecikme(atRisk));

  // 6. Burnout: kişi başına gelecek-5-gün deadline'lı aktif iş sayısı / kapasite limiti
  const BES_GUN = 5 * 24 * 3600000;
  const userById = {};
  (d.bns_users || []).forEach((u) => { userById[u.id] = u; });
  const upByKisi = {};
  aktif.forEach((b) => {
    if (b.deadline == null || b.deadline < now || b.deadline > now + BES_GUN) return;
    (b.workers || []).concat(b.leads || []).forEach((p) => {
      if (!p || !/^U/.test(p.id)) return;
      (upByKisi[p.id] = upByKisi[p.id] || { id: p.id, ad: (p.name || '').split(' ')[0] || p.name, n: 0 }).n++;
    });
  });
  const burnoutlar = Object.values(upByKisi).map((k) => {
    const u = userById[k.id] || {};
    const bo = C.bnsBurnout(k.n, C.bnsPersonCapLimit(u));
    return { ad: k.ad, pct: bo.pct, asiri: bo.asiri };
  }).filter((k) => k.asiri);
  out.push(...C.bnsSinyalBurnout(burnoutlar));
```

- [ ] **Step 2: Sözdizimi + wiring doğrula (sentetik)**

Run:
```bash
node --check scripts/firma-sinyal.js
node -e "
process.argv.push('--dry');
const { sinyalleriHesapla } = require('./scripts/firma-sinyal.js');
const now = Date.now();
const dl = (h)=>now+h*3600000;
const d = {
  bns_users:[{id:'U3',name:'Eda K',dept:'tasarim'}],
  bns_briefs: Array.from({length:8},(_,i)=>({ no:i+1, marka:'A', deadline: dl(2), durum:'calisiliyor', rev_ic:0, rev_musteri:0, workers:[{id:'U3',name:'Eda K'}], leads:[], durum_olaylari:[{ts:now-50*3600000,durum:'basladi'}] })),
  bns_completed: [ {marka:'A',sureH:5},{marka:'A',sureH:5},{marka:'A',sureH:5} ],
};
sinyalleriHesapla(d, now).then(s=>{ console.log(s.length+' sinyal'); s.forEach(x=>console.log(' ['+x.tip+'/'+(x.key||'firma')+'] '+x.text)); });
"
```
Expected: sözdizimi OK; gecikme (baseline 5h, elapsed ~50h → projMiss VEYA davranışsal) + burnout (8 iş / tasarim limiti 6 = %133 ≥120) sinyalleri görünür.

- [ ] **Step 3: Commit**

```bash
git add scripts/firma-sinyal.js
git commit -m "feat(p3.3b): gecikme öngörüsü + burnout sinyalleri (firma-sinyal motoru)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: Dashboard — "öngörülen gecikme" rozeti

**Files:**
- Modify: brief risk göstergesinin render edildiği yer (önce bul)

- [ ] **Step 1: Aktif brief'te risk/badge render yerini bul**

Run: `grep -rn "thread_ton\|bnsIsRisk\|deltaH" dashboard/app/BriefDrawer.jsx`
Expected: BriefDrawer'da rozet alanı (P3.2 thread_ton rozeti buraya eklenmişti, `_isMgr` civarı) bulunur. Bu bileşen `brief`, `currentUser`, `allUsers` prop'larına sahip.

- [ ] **Step 2: Rozeti ekle (thread_ton rozetinin yanına)**

BriefDrawer'da, thread_ton rozetinin bulunduğu bloğa, aşağıdaki rozeti ekle. Görünürlük: atanan (worker/lead) VEYA yönetici. Baseline için `data`/completed listesi BriefDrawer'da yoksa Step 3'e bak; varsa doğrudan kullan. Çağrı:
```jsx
{(() => {
  const uid = currentUser && (currentUser.slack_id || currentUser.id);
  const atanan = [...(b.workers||[]), ...(b.leads||[])].some(p => p && p.id === uid);
  if (!(_isMgr || atanan)) return null;
  const completed = (window.BNS_DATA && window.BNS_DATA.bns_completed) || [];
  const baselineH = bnsBaselineCycle(completed, b.marka);
  const elapsedH = bnsCycleSure(b.durum_olaylari || [], Date.now()).toplamH || 0;
  const o = bnsGecikmeOngoru({ deadline: b.deadline, durum: b.durum, rev_ic: b.rev_ic, rev_musteri: b.rev_musteri, elapsedH }, baselineH, Date.now());
  return o.risk ? (
    <span title={`Öngörülen gecikme (${o.sebep})`}
      style={{font:"600 10px/1 var(--font-sans)", letterSpacing:"0.04em", textTransform:"uppercase",
        color:"var(--prio-orange)", background:"rgba(255,140,0,0.1)", padding:"4px 8px", borderRadius:999}}>
      ⏳ öngörülen gecikme
    </span>
  ) : null;
})()}
```

- [ ] **Step 3: `bnsBaselineCycle`/`bnsGecikmeOngoru`/`bnsCycleSure` ve completed erişimini doğrula**

BriefDrawer bundle'da calc global'leri (`bnsBaselineCycle` vb.) erişilebilir mi kontrol et — calc.js bundle'a dahil, global olarak çağrılıyor (mevcut `bnsIsRisk`/`bnsCycleSure` kullanımına bak). Completed listesi için doğru global'i bul:
Run: `grep -rn "BNS_DATA\|bns_completed\|window.BNS" dashboard/app/*.jsx | head`
Completed'a erişim `window.BNS_DATA.bns_completed` değilse, doğru kaynağı (ör. bir `data` prop'u veya global) kullan. Erişim yoksa baseline'ı `null` geç (yalnız davranışsal sinyal çalışır) — badge yine davranışsal risk için görünür.

- [ ] **Step 4: JSX derlenebilirlik**

Run: `bash scripts/ci-check.sh 2>&1 | grep -iE "jsx|derlen|❌"`
Expected: `✅ tüm .jsx derlendi`.

- [ ] **Step 5: Commit**

```bash
git add dashboard/app/BriefDrawer.jsx
git commit -m "feat(p3.3b): BriefDrawer öngörülen gecikme rozeti (atanan+yönetici)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: Entegrasyon — CI + build + deploy + canlı doğrulama

- [ ] **Step 1: Tam CI**

Run: `bash scripts/ci-check.sh`
Expected: `🟢 CI KAPISI GEÇTİ` (yeni formül testleri + JSX + tek-tanım).

- [ ] **Step 2: Dashboard build**

Run: `bash scripts/build-dashboard.sh`
Expected: bundle + app/ + EMBEDDED senkron.

- [ ] **Step 3: Build çıktısını commit**

```bash
git add -A
git commit -m "build(p3.3b): dashboard bundle senkron (tahmin calc + gecikme rozeti)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

- [ ] **Step 4: Deploy (API + dashboard)**

```bash
bash scripts/deploy.sh api        # firma-sinyal.js scheduler servisine gider (scripts/**)
bash scripts/deploy.sh dashboard  # rozet Pages'e
```
Not: timeout olursa `git push origin main` + health bekle.

- [ ] **Step 5: Canlı --dry doğrulama**

Run: `railway run --service benseno-api node scripts/firma-sinyal.js --dry`
Expected: mevcut 5 sinyale ek olarak gecikme_ongoru + burnout sinyalleri (varsa) görünür; hata yok.

- [ ] **Step 6: Scheduler yeni kodu aldı mı**

Run: `railway logs --service benseno-tasarim-sistemi 2>&1 | grep -iE "cron|başlat" | tail -3`
Expected: `[scheduler] cron job'lar kuruldu` (yeni firma-sinyal.js ile yeniden başlamış).

---

## Self-Review

**Spec kapsama:**
- Gecikme öngörüsü (projMiss + davranışsal, marka baseline) → Task 1 (bnsGecikmeOngoru, bnsBaselineCycle) + Task 2 ✓
- Burnout (≥%120) → Task 1 (bnsBurnout) + Task 2 ✓
- Kişi-performans demeti (tamamlanan, döngü, zamanında%, revize, puan, tipDağılım, throughput) → Task 1 (bnsKisiPerformans) ✓
- 2 yeni sinyal firma-sinyal motoruna → Task 2 ✓
- Dashboard rozeti (atanan+yönetici) → Task 3 ✓
- Migration yok / tip_firma_sinyal reuse → not edildi ✓

**Spec'ten sapma (bilinçli):** baseline dept yerine **marka** (completed'da dept yok — veri-temelli düzeltme). Spec'te dept-first yazılmıştı; plan marka-only kullanır.

**Belirsizlik:** Dashboard completed erişimi (`window.BNS_DATA.bns_completed`) Task 3 Step 3'te doğrulanır; erişilemezse baseline null (davranışsal sinyal yine çalışır).

**Tip tutarlılığı:** Sinyal nesnesi `{tip, aciliyet, key, text}` P3.3a ile aynı; `key`→notifications `marka` kolonu (motor zaten böyle yazıyor).
